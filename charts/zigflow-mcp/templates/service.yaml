apiVersion: v1
kind: Service
metadata:
  name: {{ include "zigflow-mcp.fullname" . }}
  labels:
    {{- include "zigflow-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zigflow-mcp.selectorLabels" . | nindent 4 }}
