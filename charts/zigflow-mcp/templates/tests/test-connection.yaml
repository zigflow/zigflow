apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zigflow-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "zigflow-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command:
        - wget
      args:
        - 'http://{{ include "zigflow-mcp.fullname" . }}:{{ .Values.service.port }}/healthz'
  restartPolicy: Never
