document:
  dsl: 1.0.0
  taskQueue: callHTTP
  workflowType: workflow
  version: 0.0.1
do:
  - callHTTP:
      call: http
      output:
        as: '${ $context + { callHTTP: . } }'
      with:
        method: get
        endpoint: ${ "http://" + $env.HTTP_MOCK + "/posts/2" }
